cores = 'vermelho', 'Azul', 'rosa', 'Laranja'

#for b in cores:
    #print(f'{b}')

for c in range(0, len(cores)):
    print(f'Temos disponíveis a seguinte cor em estoque > {c}  ')

#for pos, i in enumerate(cores):
    #print(f'Cor {i} // posição {pos}')